name: '3a. F1 Transformation CI'

on:
  push:
    branches-ignore: [main]
    paths: ['src/transformation/f1-to-silver/**']
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths: ['src/transformation/f1-to-silver/**']

permissions:
  contents: read

jobs:
  pytest-validation:
    name: 'Unit Tests: Transformation'
    runs-on: ubuntu-latest
    steps:

    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Python      
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: 'src/transformation/f1-to-silver/requirements.txt'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      working-directory: src/transformation/f1-to-silver

    - name: Run Unit Tests
      env: { PYTHONPATH: . }
      run: python -m pytest -v
      working-directory: src/transformation/f1-to-silver